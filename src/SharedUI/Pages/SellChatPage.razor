@page "/Sell/Chat"

<div class="Stage" style="padding:14px">
    <h1 style="font-size:1.4rem;margin-bottom:6px">Chat with Agent</h1>
    <p style="color:var(--text-muted);font-size:.9rem;margin-bottom:14px">Tell the agent what you're shipping. He'll write the job posting for you.</p>

    <div style="display:flex;flex-direction:column;gap:10px;margin-bottom:14px">
        @foreach (var Pair in VisiblePairs)
        {
            <div style="align-self:flex-start;max-width:80%;background:#f3f6fa;border:1px solid var(--border);border-radius:14px 14px 14px 4px;padding:10px 14px;font-size:.9rem">
                <strong style="color:var(--accent);font-size:.74rem;display:block;margin-bottom:2px">Agent</strong>
                @Pair.Question
            </div>
            <div style="align-self:flex-end;max-width:80%;background:var(--accent);color:#fff;border-radius:14px 14px 4px 14px;padding:10px 14px;font-size:.9rem">
                <strong style="font-size:.74rem;display:block;margin-bottom:2px;opacity:.85">You</strong>
                @Pair.Answer
            </div>
        }
        @if (Step >= 4)
        {
            <div style="align-self:flex-start;max-width:90%;background:#fff5e6;border:1px solid var(--accent);border-radius:14px;padding:10px 14px;font-size:.86rem">
                <strong style="color:var(--accent);font-size:.74rem;display:block;margin-bottom:4px">Agent · proposal</strong>
                <div style="font-weight:700">2024 BYD Han EV — Hefei to Wilmington</div>
                <div style="color:var(--text-muted);font-size:.78rem;margin-top:2px">$48,500 · pay on delivery · 4-leg international relay · cash to factory $18,000.</div>
                <button class="Btn" style="margin-top:8px;font-size:.8rem;padding:6px 12px;min-height:34px">Looks good — publish job</button>
            </div>
        }
    </div>

    <div style="display:flex;gap:8px;align-items:center;background:#fff;border:1px solid var(--border);border-radius:24px;padding:6px 10px">
        <input type="text" style="flex:1;border:none;outline:none;padding:10px;font-size:.9rem" placeholder="Type your reply…" />
        <button class="Btn" style="border-radius:50%;width:44px;height:44px;padding:0;font-size:1rem;flex:none">🎙️</button>
    </div>
</div>
