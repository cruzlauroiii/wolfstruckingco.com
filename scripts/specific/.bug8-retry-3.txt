feedback_tasks_run_immediately.md:14:- **EVERY task gets an agent — including blocked tasks.** A blocked task still needs a background agent that proceeds with whatever it CAN do (planning, prep work, scoping, drafting) without touching the blocking work's output. The blocked agent does the planning so when the blocker clears, execution is instant. **User reinforced this 2026-05-01: "all task should be parallel sub agent in the background even there is a blocker."** No deferral, no "wait for blocker" excuse — spawn the agent and let it do prep work in parallel.
